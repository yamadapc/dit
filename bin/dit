#!/usr/bin/env node
'use strict';
/**
 * Dependencies
 * --------------------------------------------------------------------------*/

var cli  = require('../lib/cli'),
    argv = require('optimist').argv;

/**
 * Run the command-line client
 */

cli.argv(argv);
